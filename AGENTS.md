# Coding Agent Guide

이 저장소의 목표는 현대해상 신규위험 탐색 실무자가 `신호 → 후보 → 평가 → 리포트`를 일관되게 다루고, 고객 채널의 미충족 위험을 개인정보 없이 연구 신호로 돌려받는 제품 프레임을 만드는 것입니다.

## 작업 전 읽기 순서

1. `README.md`
2. `develop-footprint/current-feature-classification.md`
3. 담당 기능 문서
4. `develop-footprint/platform/integration-contracts.md`
5. `develop-footprint/platform/service-boundaries.md`

## 변경 규칙

- 안정 경로 `/`, `/risks`, `/risks/:riskId`, `/reports`, `/customer-insight`를 임의로 바꾸지 않습니다.
- `/customer-insight`는 `CustomerLayout`, 나머지 실무자 경로는 `RootLayout`을 사용합니다. 고객 기능을 실무자 좌측 내비게이션 안에 다시 넣지 않습니다.
- 메뉴 활성 상태는 `isNavigationItemActive()`를 사용하며 한 경로에서 `aria-current="page"`는 하나만 허용합니다.
- 공통 데이터 계약은 `src/domain`을 단일 기준으로 사용합니다. 페이지 내부에 중복 타입을 만들지 않습니다.
- 페이지는 조합 계층으로 유지하고 분석·저장 로직은 `src/features`, 재사용 계약은 `src/domain`, 공통 표현은 `src/shared`에 둡니다.
- 색상·간격·그림자는 `src/styles/global.css`의 CSS 변수로 확장합니다. 공식 Hi Orange/Hi Navy 값은 변경하지 않습니다.
- 현대해상 로고나 `현대해상 마음체` 파일을 복제·번들하지 않습니다. 승인된 자산이 제공될 때만 교체합니다.
- 샘플 수치와 가상 판단은 화면에 `SAMPLE` 또는 `샘플 표시`를 붙입니다. 실제 내부 데이터처럼 표현하지 않습니다.
- 상품 보장, 면책, 보험료, 가입 가능 여부를 코드에서 단정하지 않습니다. 공식 문서 링크와 확인 필요 항목을 함께 제공합니다.
- 고객 이름, 연락처, 주민등록번호, 상세 주소, 자유입력 원문을 분석가 후보 데이터에 저장하지 않습니다.
- 단일 고객 신호를 신규위험 후보로 자동 승격하지 않습니다. 운영 환경에서는 최소 집계 기준과 사람의 검토를 거칩니다.
- 외부 API 키, 크롤러 인증정보, 모델 키는 브라우저 번들에 넣지 않습니다. 서버 경계 뒤에 둡니다.
- 다른 담당 영역을 대규모 재작성하기 전에 계약 변경을 먼저 문서화하고 영향 경로를 공유합니다.

## 기능 완료 기준

- 로딩, 빈 상태, 오류, 권한 없음 중 해당 상태를 처리합니다.
- 데이터 최신 시각과 출처를 사용자가 확인할 수 있습니다.
- 판단 결과에 사용한 근거 ID, 불확실성, 반증 또는 보류 이유를 남길 수 있습니다.
- 키보드 포커스, 의미 있는 레이블, 모바일 레이아웃을 유지합니다.
- `npm run check`가 통과합니다.
- 구현 상태와 다음 의존성을 `develop-footprint`에서 갱신합니다.

## 병합 기준

각 기능 팀은 화면 컴포넌트뿐 아니라 해당 도메인 어댑터, 오류/빈 상태, 샘플 데이터 구분, 문서 변경을 한 슬라이스로 제출합니다. 공통 계약 변경은 소비자 페이지를 함께 수정하며, 기능 브랜치에서 새 전역 스타일이나 새 저장 방식을 독자적으로 만들지 않습니다.

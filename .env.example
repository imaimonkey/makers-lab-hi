# Vite client-safe settings. The mock is enabled by default for local UI testing.
VITE_LLM_USE_MOCK=true
VITE_LLM_API_BASE_URL=/api/llm

# Server-only Gemini settings. Do not rename these to VITE_* or expose them to the browser.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
